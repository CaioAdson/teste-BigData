agent.sources = source1
agent.channels = channel1
agent.sinks = logger1
agent.sources.source1.type = TAILDIR
agent.sources.source1.positionFile = /state/taildir-position.json
agent.sources.source1.filegroups = group1
agent.sources.source1.filegroups.group1 = /data/events.jsonl
agent.sources.source1.channels = channel1
agent.channels.channel1.type = memory
agent.channels.channel1.capacity = 1000
agent.channels.channel1.transactionCapacity = 100
agent.sinks.logger1.type = logger
agent.sinks.logger1.channel = channel1
